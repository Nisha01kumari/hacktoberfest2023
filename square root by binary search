class solution 
{
public;
int binary search (intn)
{
int s = 0;
int e = n;
long long int = s+(e-s)/2;
long long int ans = -1;
while(s<=e)
{
int square = mid*mid;
if(square == n)
return mid;
if(square =< n)
{
ans = mid;
s = mid +1;
}
else 
{
e = mid-1;
}
mid = s+(e-s)/2
}
return ans;
}
int mysqrt(int n)
{
return binary search(x);
}
